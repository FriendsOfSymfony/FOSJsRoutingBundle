Routing.prefix = '{{ prefix }}';
Routing.variablePrefix = '{{ var_prefix }}';
Routing.variableSuffix = '{{ var_suffix }}';
{% for name, route in exposed_routes %}
Routing.connect({{ name|json_encode|raw }}, {{ route.pattern|json_encode|raw }}, {{ route.defaults|json_encode|raw }});
{% endfor %}
